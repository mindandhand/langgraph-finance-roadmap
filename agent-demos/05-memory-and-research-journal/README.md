# 05：研究记忆不是聊天历史

量化研究里的 memory 不应该只保存聊天上下文。更有价值的是 research journal：做过什么实验、用过哪些参数、产物在哪里、失败原因是什么。

## 运行

```bash
python research_journal.py
```

脚本会写出一个 JSONL journal，记录数据加载、因子计算、评估结论。

## 新增概念

长期记忆应该结构化，能被审查和复现，而不是一大段自然语言聊天记录。
