# 06：规划一个有预算的因子研究

Agent 很容易无限尝试。量化研究 Agent 必须先生成计划，并限制预算：最多几个因子、几个窗口、几次回测、是否允许看最终测试集。

## 运行

```bash
python factor_study_plan.py
```

本例生成一个固定研究计划，并检查是否超出预算。

## 新增概念

计划不是建议列表，而是执行约束。后续 Agent 只能在计划允许的范围内调用工具。
